# v1.5.1 — Release Checklist (TestFlight → App Store)

**Target submission**: when every gate below is ✅ or has a written waiver.
**Owner**: pdythanhduy.
**Source ref**: `main` HEAD `0c4ac3f` (chore: growth asset pack v1) — or later if more commits land.
**Predecessor**: v1.5.0 build 26 (APPROVED, live on App Store).
**Scope reference**: [`docs/product-state-v1.5.1-prep.md`](product-state-v1.5.1-prep.md) — what v1.5.1 SHOULD ship (§2) and what it MUST NOT (§3).
**Operating constraint**: [`docs/observation-season-freeze.md`](observation-season-freeze.md) — freeze ACTIVE until 2026-06-12 minimum. Read §22-35 before approving any addition.

This is a **gate**, not a guide. Do not submit until every item is ✅ or waived in §11.

---

## 0. Scope ceiling (read first)

v1.5.1 is a **foundation release**, not a feature release. The branch already-merged delivers:
- Analytics wiring fixes (3 wrappers + 1 missing source) — `product-state-v1.5.1-prep.md:24`
- HomeScreen light modularization (`getBookmarkMeta` extraction) — `product-state-v1.5.1-prep.md:25`
- Retention foundation tests + R3 gate doc — `product-state-v1.5.1-prep.md:26-27`
- 8 v1.5.1 search query pins + 1 keyword phrase — `product-state-v1.5.1-prep.md:28-29`
- Growth next-loop doc + this prep doc — `product-state-v1.5.1-prep.md:30-31`

**Anything else in this build = scope creep.** Push back or hold for v1.5.2.

The "MUST NOT ship" list (`product-state-v1.5.1-prep.md:50-69`) is enforced by this checklist — see §6 grep below.

---

## 1. Version & build number

Repo currently:
- `package.json` → `"version": "1.5.0"`
- `app.json` → `"expo.version": "1.5.0"`
- `eas.json` → `cli.appVersionSource: "remote"`, `build.production.autoIncrement: true`
- `app.json` → `expo.runtimeVersion.policy: "appVersion"` (runtime ties to `expo.version`)

Action:
- [ ] Bump `package.json` → `"version": "1.5.1"`
- [ ] Bump `app.json` → `"expo.version": "1.5.1"`
- [ ] **Do NOT manually set** `expo.ios.buildNumber` or `expo.android.versionCode` — EAS Build server auto-increments via `appVersionSource: remote` (verify on EAS dashboard before submitting)
- [ ] Expected next iOS build number: **27** (v1.5.0 used build 26)
- [ ] Bundle identifier UNCHANGED: `com.thanhduy.camnangvietnhat` (`app.json:16, 27`)

**OTA vs new binary**: `runtimeVersion.policy: appVersion` (`app.json:54-56`) means bumping `expo.version` creates a new runtimeVersion (`1.5.0` → `1.5.1`). v1.5.0 native shells cannot pull v1.5.1 OTA. → **Must ship as new binary**. OTA-only path is not available without keeping version at 1.5.0 (rejected — release-checklist gate 3 requires version bump).

---

## 2. Code gates (from `product-state-v1.5.1-prep.md` §4)

All from `product-state-v1.5.1-prep.md:76-83`:

- [ ] `npx tsc --noEmit` exits 0
- [ ] `npm run test:ci` — all suites pass, **suite count ≥ 52**, **test count ≥ 340** (baseline + 12 new)
- [ ] `npm run verify:content` — encoding audit + BJT QA gate, zero warnings
- [ ] `git diff package.json` — zero changes in `dependencies` / `devDependencies` blocks (only `version` field allowed)
- [ ] Grep: no new `console.warn` / `console.error` outside `if (__DEV__)` guards
- [ ] Grep: no bare `any`, no new `@ts-expect-error` without comment

Run all in one shot:
```bash
npm run verify   # = typecheck + test:ci + verify:content
```

---

## 3. Privacy gates (from `product-state-v1.5.1-prep.md` §4)

- [ ] Every new event in `EventMap` has bounded enum properties (no free-text user input)
- [ ] Specifically verify: `home_search_pressed: void` (no payload), `home_quick_action_pressed.action_id` is a 7-value union (`product-state-v1.5.1-prep.md:87`)
- [ ] `docs/analytics-events.md` describes WHEN each new event fires
- [ ] `docs/analytics-decision-map.md` maps each new event to a product question
- [ ] `search_query` event NEVER contains diacritics, mixed case, or >24-char strings — `normalizeQueryForAnalytics` 22-test suite passes (`product-state-v1.5.1-prep.md:110`)

---

## 4. Smoke test (real device, iOS first)

Reuse [`docs/v1.5.0-release-checklist.md`](v1.5.0-release-checklist.md) §1, plus v1.5.1 specifics:

- [ ] App launches cold from home screen in < 4s
- [ ] Tab navigation between Home / Admin / Jobs / Japanese / Saved — no crash, no white flash
- [ ] Open and close `AdminDetail` for 5 different guides — no leak/stutter on back nav
- [ ] Daily Ritual: tap "Bắt đầu" → answer 3 → finish → streak increments
- [ ] Bookmark a guide → close app → reopen → bookmark persists
- [ ] Search empty + no-results states still render featured guides (Phase 2B unchanged)
- [ ] Featured guide tap fires `guide_open { source: 'featured' }`
- [ ] Related guide tap fires `guide_open { source: 'related' }`
- [ ] Search result tap fires `guide_open { source: 'search' }`
- [ ] Direct nav fires `guide_open { source: 'direct' }`

**NEW for v1.5.1** (verify analytics wiring fixes from `product-state-v1.5.1-prep.md:24`):

- [ ] Tap search field on Home → `home_search_pressed` event fires in Metro log (was no-op in v1.5.0)
- [ ] Tap any quick action on Home → `home_quick_action_pressed { action_id }` fires with correct enum value (was no-op)
- [ ] Tap "Bắt đầu ở đâu?" entry chip → `home_start_here_pressed` fires (was no-op)
- [ ] Tap cold-start emergency chip → `emergency_cta_open { source: 'home_start_here' }` fires (was silently dropped)

---

## 5. Observation-season constraint check

Per `observation-season-freeze.md:22-35`:

- [ ] No new analytics event added (current 8 events frozen — `observation-season-freeze.md:15`)
- [ ] No new search heuristic (abandon 10s, debounce 800ms, min query 3 chars, searcher threshold 5 — all frozen — `observation-season-freeze.md:16`)
- [ ] No new keyword pin without 2 consecutive weekly reports showing count ≥ 5 + DAU ≥ 30 (`observation-season-freeze.md:30`)
- [ ] No new dashboard chart (the 7 charts in `aptabase-phase2c-dashboard-checklist.md` are frozen)
- [ ] No new operating-framework doc (templates frozen at v1.5.4)

**Exception list** (`observation-season-freeze.md:65-71`): bug fixes, content edits per content-governance, security fixes, doc maintenance that fixes stale refs/dates, audits. None apply to v1.5.1 binary scope.

---

## 6. Hidden-feature verification

These MUST stay hidden (from `v1.5.0-release-checklist.md:48-61`):

- [ ] AI Mail (Dịch Thư Nhật) — no Home/Settings entry point
- [ ] My Japan Plan (#35) — no Home exposure, no nav entry
- [ ] JLPT — no menu entry
- [ ] No debug screens in nav

Grep gates (must return ONLY gating comments, NOT render paths):
```bash
grep -rn "MailCapture\|MailTranslate" src/screens/HomeScreen.tsx src/screens/home/
grep -rn "MyJapanPlan\|PersonalPlan\|JourneyChecklist" src/screens/HomeScreen.tsx
```

---

## 7. Build & submit (EAS)

App Store Connect app ID: `6762459919` (`eas.json:21`).

Build command (production iOS):
```bash
eas build --platform ios --profile production
```

- [ ] EAS dashboard shows new build with version `1.5.1` and build number `27` (auto-incremented)
- [ ] Build artifact downloaded + smoke-tested via TestFlight on at least 1 device
- [ ] TestFlight build added to internal testing group
- [ ] After ≥ 2 hours TestFlight smoke (no crash, key flows work):

Submit:
```bash
eas submit --platform ios --profile production --latest
```

- [ ] Submission triggered to ASC app `6762459919`
- [ ] ASC build status: "Waiting for Review" within 1 hour
- [ ] Submission ID recorded in this file's §10 below

**Android**: per `product-state-v1.5.1-prep.md:17`, Android v1.5.0 NOT yet built. v1.5.1 Android decision is **deferred** unless explicitly greenlit in §11.

---

## 8. App Review notes (paste in ASC)

Use exactly this text in App Store Connect → Version → App Review Information → Notes:

```
v1.5.1 is a foundation maintenance release on top of v1.5.0 (approved build 26).

Changes from v1.5.0:
- Analytics wiring fixes (4 events that were no-op in v1.5.0 now fire correctly)
- Search regression-pin tests added (8 new pinned queries)
- Internal documentation and design specs (R3 reminder gate, growth roadmap)
- One new keyword phrase added to the "lost residence card" guide

No new user-visible features. No new permissions. No new dependencies.
Same login flow as v1.5.0 (no login required — app is fully offline-capable).
Same demo account: not applicable (no auth).

Privacy: no new data collected. The aggregate search keywords sent to Aptabase
remain 24-char normalized with no diacritics, no raw queries, no PII.

Hidden routes (AI Mail, My Japan Plan, JLPT) remain unreachable from the UI.
```

- [ ] Notes text pasted in ASC
- [ ] No demo account fields filled (we don't have auth)
- [ ] Contact info: pdythanhduy@gmail.com (or current value)

---

## 9. Metadata decision (couple with ASO ship?)

**Two paths** — pick one before submitting binary:

**Path A — Binary only, metadata UNCHANGED**
- [ ] Reuse v1.5.0 metadata (screenshots, description, subtitle, keywords)
- [ ] Faster to submit, but loses the ASO refresh opportunity bundled with this build
- [ ] Use if observation season findings haven't validated ASO direction

**Path B — Binary + ASO refresh bundled** *(recommended per `v1.5.0-app-store-audit.md:158` "marketing assets benefit from actual UX being live + at least some user feedback")*
- [ ] Follow [`docs/reports/aso-submission-2026-05-23.md`](reports/aso-submission-2026-05-23.md) (companion doc)
- [ ] Subtitle update + keyword refresh + 6 new screenshots — all metadata-only changes
- [ ] Adds ~2 days to ship timeline but ships with binary in single review cycle

**Picked path**: ☐ A   ☐ B (mark before binary submission)

---

## 10. Submission record

Fill after `eas submit` completes:

| Field | Value |
| --- | --- |
| Submission date | _____________ |
| Submission ID (EAS) | _____________ |
| iOS build number | 27 (expected) |
| Apple submission ID (ASC) | _____________ |
| Review queue entered | _____________ |
| App Review result | _____________ |
| Live on App Store | _____________ |

---

## 11. Waivers

If any item above is intentionally skipped, write the reason here BEFORE submission:

| Item | Reason for waiver | Approved by |
| --- | --- | --- |
| (none yet) | | |

---

## 12. Rollback plan

If App Review rejects:
- Read rejection reason carefully — most v1.5.1 changes are non-binary
- For analytics-only issues: respond in Resolution Center without code change (clarify the wiring fixes)
- For binary issues: revert to tag `v1.5.0` (commit `cc25e63` per memory), open hotfix branch off main
- Re-submit as v1.5.2 (skip v1.5.1 number to avoid binary version collision)

If post-launch crash > 1% sessions (`product-state-v1.5.1-prep.md:144`):
- Identify via App Store Connect Crash Analytics
- Hotfix branch from main
- Re-submit as v1.5.2

---

## Related docs

- [`docs/product-state-v1.5.1-prep.md`](product-state-v1.5.1-prep.md) — scope source of truth
- [`docs/v1.5.0-release-checklist.md`](v1.5.0-release-checklist.md) — template / precedent
- [`docs/v1.5.0-changelog.md`](v1.5.0-changelog.md) — for App Review notes pattern
- [`docs/observation-season-freeze.md`](observation-season-freeze.md) — operating constraints
- [`docs/reports/aso-submission-2026-05-23.md`](reports/aso-submission-2026-05-23.md) — companion ASO ship plan (Path B)
- [`docs/reminder-system-design.md`](reminder-system-design.md) §8b — R3 gate decision date 2026-06-04
